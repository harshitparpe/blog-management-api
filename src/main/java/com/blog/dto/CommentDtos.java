package com.blog.dto; import jakarta.validation.constraints.*; import java.time.Instant; import java.util.*; public final class CommentDtos { private CommentDtos(){} public record CommentRequest(@NotBlank @Size(min=1,max=5000) String content,Long parentCommentId){} public record CommentResponse(Long id,String content,Instant createdAt,AuthDtos.UserResponse user,List<CommentResponse> replies){} }
