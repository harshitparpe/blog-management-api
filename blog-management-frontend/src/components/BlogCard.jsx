import {Link} from 'react-router-dom';import {Calendar,UserRound,ArrowUpRight} from 'lucide-react';const date=d=>new Intl.DateTimeFormat(undefined,{dateStyle:'medium'}).format(new Date(d));export default function BlogCard({post}){return <article className="card group flex flex-col overflow-hidden transition hover:-translate-y-1 hover:shadow-lg">{post.imageUrl&&<img src={post.imageUrl} alt="" className="h-44 w-full object-cover"/>}<div className="flex flex-1 flex-col p-5"><div className="mb-3 text-xs font-semibold uppercase tracking-wide text-brand">{post.category?.name||'Uncategorized'}</div><h2 className="font-[Playfair_Display] text-xl font-bold leading-snug"><Link className="hover:text-brand" to={`/posts/${post.id}`}>{post.title}</Link></h2><p className="mt-3 line-clamp-3 flex-1 text-sm leading-6 text-slate-600 dark:text-slate-400">{post.content}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-500 dark:border-slate-800"><span className="flex items-center gap-1"><UserRound size={14}/>{post.author?.username}</span><span className="flex items-center gap-1"><Calendar size={14}/>{date(post.createdAt)}</span></div><Link to={`/posts/${post.id}`} className="mt-4 flex items-center gap-1 text-sm font-semibold text-brand">Read story <ArrowUpRight size={16}/></Link></div></article>}
