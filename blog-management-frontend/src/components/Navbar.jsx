import {Link,NavLink,useNavigate} from 'react-router-dom';import {Moon,Sun,PenSquare,Menu,X,LogOut,LayoutDashboard} from 'lucide-react';import {useState} from 'react';import {useAuth} from '../context/AuthContext';
export default function Navbar(){const {user,logout,isRole}=useAuth(),[open,setOpen]=useState(false),[dark,setDark]=useState(()=>document.documentElement.classList.contains('dark'));const nav=useNavigate();const toggle=()=>{document.documentElement.classList.toggle('dark');setDark(!dark)};const leave=()=>{logout();nav('/')};const links=<><NavLink to="/" onClick={()=>setOpen(false)}>Explore</NavLink>{isRole('AUTHOR','ADMIN')&&<NavLink to="/posts/new">Write</NavLink>}{isRole('ADMIN')&&<NavLink to="/categories">Categories</NavLink>}</>;return <header className="sticky top-0 z-20 border-b border-slate-200/80 bg-slate-50/90 backdrop-blur dark:border-slate-800 dark:bg-slate-950/90"><div className="container-page flex h-16 items-center justify-between"><Link to="/" className="font-[Playfair_Display] text-2xl font-bold text-brand">Inkspire.</Link><nav className="hidden items-center gap-6 text-sm font-medium md:flex">{links}</nav><div className="hidden items-center gap-3 md:flex"><button className="btn-secondary !p-2" onClick={toggle} aria-label="Toggle theme">{dark?<Sun size={18}/>:<Moon size={18}/>}</button>{user?<><Link to="/dashboard" className="btn-secondary"><LayoutDashboard size={17}/> Dashboard</Link><button onClick={leave} className="btn-primary"><LogOut size={17}/> Logout</button></>:<><Link to="/login" className="btn-secondary">Sign in</Link><Link to="/register" className="btn-primary">Get started</Link></>}</div><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="container-page flex flex-col gap-4 border-t border-slate-200 py-4 font-medium md:hidden">{links}{user?<><Link to="/dashboard">Dashboard</Link><button className="text-left" onClick={leave}>Logout</button></>:<Link to="/login">Sign in</Link>}</div>}</header>}
