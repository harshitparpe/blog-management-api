import axios from 'axios'; import toast from 'react-hot-toast';
const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://localhost:8080/api',headers:{'Content-Type':'application/json'}});api.interceptors.request.use(c=>{const t=localStorage.getItem('blog_token');if(t)c.headers.Authorization=`Bearer ${t}`;return c;});api.interceptors.response.use(r=>r,e=>{if(e.response?.status===401){localStorage.removeItem('blog_token');localStorage.removeItem('blog_user');if(location.pathname!=='/login')location.assign('/login');}else if(!e.response)toast.error('Network error. Please check your connection.');return Promise.reject(e);});export default api;
