import api from '../api/axiosConfig'; export const getPosts=p=>api.get('/posts',{params:p}).then(r=>r.data);export const searchPosts=(keyword,p)=>api.get('/posts/search',{params:{keyword,...p}}).then(r=>r.data);export const getPost=id=>api.get(`/posts/${id}`).then(r=>r.data);export const createPost=d=>api.post('/posts',d).then(r=>r.data);export const updatePost=(id,d)=>api.put(`/posts/${id}`,d).then(r=>r.data);export const deletePost=id=>api.delete(`/posts/${id}`);
